import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import Text from '../components/Text';

ReactDOM.render(
  <Text label={'hong'} text={'jiawei'} />,
  document.getElementById('app')
);
